(function() {
	tuna.ui.modules.register('popup', {
		init: function (target, container, a, b) {
            debugger;
            
            $('.j-popup', target).each(function() {
            });
		}
	});
})();